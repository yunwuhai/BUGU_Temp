/*************
引用类：方法c文件引用其所属的类h文件
*************/
#include "../inc/0_0_Trans_0_SYS.h"
/*************
方法重载：方法c文件内部不止一个函数，而是多个函数，每个函数都对应了一个方法重载
命名：方法名_重载id_重载英语名
重载英语名空白可省略
前端编程的时候实际上是对方法重载进行的编辑，而方法本身只是一个分类。
*************/
void Func_0_0_Trans_0_SYS_0_MCU_Configure_0_Basic()
{
    /*************
    函数调用：实际上就是函数调用，在前端展示的时候是某一个对象使用方法，
    但是在后端解析时我们不需要在意那个对象，而只用关注使用的方法即可，
    通过用户id、组件id、类id（使用方法的那个对象所属的类）、方法id和重载id，我们即可知道每一步使用的函数名，然后填充上去。
    在前端我们添加方法时，需要先选择需要添加的行，然后从对象栏选择使用方法的对象，然后跳转到方法栏中该对象所属类的方法区域，然后选择方法即可。
    此处尚未介绍有参数情况。
    *************/
    Func_0_0_Trans_2_CLK_0_MCU_Configure_0_Basic();
    Func_0_0_Trans_1_INT_0_MCU_Configure_0_Basic();
    Func_0_0_Trans_6_FLASH_2_MCU_Configure_0_Basic();
    Func_0_0_Trans_5_PWR_0_MCU_Configure_0_Basic();
    /*************
    返回语句：每个方法重载在完成创建后，如果其最后一个方法不是方法结束方法，那么将自动显示添加一个方法结束方法（如果是则不需要添加）。
    后端将会把方法结束方法解析为return语句。
    *************/
    return;
}