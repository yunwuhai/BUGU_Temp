/* 防止多次重复引用，每个头文件都需要生成改内容
   宏定义内容命名方法为，__组件英语名大写_作者ID_组件ID_类ID__,
   本文件的文件名也是同理。 */
#ifndef __GPIO_0_2_0_H__
#define __GPIO_0_2_0_H__
/*为了便于移植到C++需要使用这个定义，事实上也可以不管，但是为了规范我们还是自动生成一下*/
#ifdef __cplusplus
extern "C" {
#endif


/*为了便于移植到C++需要使用这个定义，事实上也可以不管，但是为了规范我们还是自动生成一下*/
#ifdef __cplusplus
}
#endif

/*结束开头的ifndef，自动生成即可*/
#endif
